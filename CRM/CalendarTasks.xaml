<Window x:Class="CRM.CalendarTasks"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CalendarTasks" Height="300" Width="402">
    <Grid HorizontalAlignment="Left" Width="392">
        <DataGrid AutoGenerateColumns="False" 
          Height="270" 
          HorizontalAlignment="Left" 
          Name="dataGrid1" 
          VerticalAlignment="Top" 
          Width="382">
            <DataGrid.Columns >
                <DataGridTextColumn Binding="{Binding Path=A}" MinWidth="60" />
                <DataGridTextColumn Binding="{Binding Path=B}" MinWidth="60" />
                <DataGridTextColumn Binding="{Binding Path=C}" MinWidth="60" />
                <DataGridTextColumn Binding="{Binding Path=D}" MinWidth="60" />
                <DataGridTextColumn Binding="{Binding Path=E}" MinWidth="60" />
                <DataGridTextColumn Binding="{Binding Path=F}" MinWidth="60" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
